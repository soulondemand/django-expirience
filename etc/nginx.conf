server {
	listen 80 default_server;
	server_name _;

	access_log /home/box/nginx.access.log main;
	error_log  /var/log/nginx.error_log  info;	


	location ^~ /uploads/ {
		alias /home/box/web/uploads/;
	}

	location ~ .*\..+ {
		root /home/box/web/public/;
	}

	location ~ / {
		return 404;
	}


}
